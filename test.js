const { teno } = require("./index");

teno.log("update available 0.0.1 -> 0.0.2", "if you want to upgrade, run below command", "npm i -g dizipal-cli");
teno.log("~b update on available 0.0.1 -> 0.0.2~", "~gb,b npm i -g dizipal-cli~");